{
  "mcpServers": {
    "postgres-wins": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--network",
        "wins_hub_wins_net",
        "-e",
        "DATABASE_URI=postgresql://claude_ro:leitura_wins_2026@wins_hub-db-1:5432/wins_hub",
        "crystaldba/postgres-mcp",
        "--access-mode=restricted"
      ]
    }
  }
}